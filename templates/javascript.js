// Write your solution below
const input = [];
require('readline')
  .createInterface(process.stdin, process.stdout)
  .on('line', line => input.push(line))
  .on('close', () => {
    // Your code here
  });
